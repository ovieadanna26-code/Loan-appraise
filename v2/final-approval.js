const app=document.getElementById('app');let db=null,user=null;const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const num=v=>Number(String(v??'').replace(/,/g,''))||0;const money=v=>'₦'+num(v).toLocaleString('en-NG',{maximumFractionDigits:0});async function profile(){const {data}=await db.from('profiles').select('id,role,full_name,email').eq('id',user.id).maybeSingle();return data}function login(){app.innerHTML='<div class="login"><div class="login-card"><h1>Loan<span>Appraise</span></h1><p class="muted">Final Approval</p><form id="f"><div class="field"><label>Email</label><input id="email" type="email" required></div><div class="field"><label>Password</label><input id="password" type="password" required></div><div id="err" class="error"></div><button class="primary">Sign In</button></form></div></div>';document.getElementById('f').onsubmit=async e=>{e.preventDefault();const {data,error}=await db.auth.signInWithPassword({email:email.value.trim(),password:password.value});if(error){err.textContent=error.message;return}user=data.user;render()}}async function render(){const p=await profile();if(!p||!['admin','final_approver','supervisor'].includes(p.role)){app.innerHTML='<div class="login"><div class="login-card"><h2>Access denied</h2><p class="error">Final Approval access is restricted.</p></div></div>';return}app.innerHTML='<div class="shell"><aside class="side"><div class="brand">Loan<span>Appraise</span></div><div class="nav"><button class="active">Final Approval</button><button id="out">Sign Out</button></div></aside><main class="main"><div class="top"><div><h1>Final Approval</h1><div class="muted">Review supervisor-approved applications</div></div></div><div class="card"><h2>Approval Queue</h2><div class="table-wrap"><table><thead><tr><th>Reference</th><th>Customer</th><th>Requested</th><th>Supervisor Approved</th><th>Final Approved Amount</th><th>Decision</th></tr></thead><tbody id="rows"><tr><td colspan="6">Loading…</td></tr></tbody></table></div></div></main></div>';document.getElementById('out').onclick=async()=>{await db.auth.signOut();location.reload()};load()}async function load(){const {data,error}=await db.from('loan_applications').select('*, customers(*)').eq('status','approved').order('approved_at',{ascending:false});if(error){rows.innerHTML='<tr><td colspan="6" class="error">'+esc(error.message)+'</td></tr>';return}if(!data?.length){rows.innerHTML='<tr><td colspan="6" class="muted">No applications awaiting final approval.</td></tr>';return}rows.innerHTML=data.map(a=>{const c=a.customers||{};return '<tr><td>'+esc(a.reference)+'</td><td>'+esc(c.full_name||'—')+'</td><td>'+money(a.amount)+'</td><td>'+money(a.recommended_amount)+'</td><td><input class="final" data-id="'+esc(a.id)+'" value="'+(a.final_approved_amount?num(a.final_approved_amount).toLocaleString('en-NG'):'')+'" placeholder="0" inputmode="numeric"></td><td><button class="primary approve" data-id="'+esc(a.id)+'">Approve</button> <button class="secondary decline" data-id="'+esc(a.id)+'">Decline</button></td></tr>'}).join('');document.querySelectorAll('.final').forEach(x=>x.oninput=()=>x.value=x.value.replace(/[^0-9]/g,'').replace(/\B(?=(\d{3})+(?!\d))/g,','));document.querySelectorAll('.approve').forEach(b=>b.onclick=()=>decide(b.dataset.id,true));document.querySelectorAll('.decline').forEach(b=>b.onclick=()=>decide(b.dataset.id,false))}async function decide(id,approved){const input=document.querySelector('.final[data-id="'+CSS.escape(id)+'"]');const amount=num(input.value);const max=num(input.closest('tr').children[3].textContent);if(approved&&(!amount||amount>max)){alert('Final approved amount is required and cannot exceed the Supervisor approved amount.');input.focus();return}let comments=null,conditions=null,reason=null;if(approved){comments=prompt('Final approval comments (optional):')||null;conditions=prompt('Approval conditions (optional):')||null}else{reason=prompt('Reason for final decline:');if(!reason?.trim())return}const patch=approved?{final_approver_id:user.id,final_approval_status:'approved',final_approved_amount:amount,final_approval_comments:comments,final_approval_conditions:conditions,final_approved_by:user.id,final_approved_at:new Date().toISOString(),status:'approved',updated_at:new Date().toISOString()}:{final_approver_id:user.id,final_approval_status:'declined',final_declined_reason:reason.trim(),final_declined_at:new Date().toISOString(),status:'declined',updated_at:new Date().toISOString()};const {error}=await db.from('loan_applications').update(patch).eq('id',id);if(error){alert('Could not save final decision: '+error.message);return}alert(approved?'Final approval saved.':'Application declined.');load()}(async()=>{if(window.supabase?.createClient)db=window.supabase.createClient(window.SUPABASE_URL,window.SUPABASE_ANON_KEY);if(!db){app.textContent='Authentication service unavailable.';return}const {data}=await db.auth.getSession();if(data.session){user=data.session.user;render()}else login()})();