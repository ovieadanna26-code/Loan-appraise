const app=document.getElementById('app');let client=null,session=null,profile=null,step=0;const state={};const steps=['Customer','Loan Request','Business & Sales','Financials','Products','Balance Sheet','Collateral','Review'];const moneyFields=new Set(['amount','average_daily_sales','monthly_purchases','rent','salaries','utilities','transport','other_business_expenses','cash_at_hand','bank_balance_1','accounts_receivable','supplier_liabilities','bank_loans','other_borrowings','equipment_value','vehicle_value','property_value','other_assets','market_value','forced_sale_value']);const n=v=>Number(String(v??'').replace(/,/g,''))||0;const money=v=>'₦'+n(v).toLocaleString('en-NG',{maximumFractionDigits:0});const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function input(k,label,type='text',req=false){return `<div class="field"><label>${label}${req?' *':''}</label><input data-key="${k}" type="${moneyFields.has(k)?'text':type}" value="${esc(moneyFields.has(k)?(state[k]===''||state[k]==null?'':n(state[k]).toLocaleString('en-NG')):state[k]||'')}" inputmode="${moneyFields.has(k)||['nin','bvn'].includes(k)?'numeric':type==='tel'?'tel':'text'}" ${req?'required':''}></div>`}function calc(){state.monthly_sales=n(state.average_daily_sales)*n(state.business_days);state.total_assets=['equipment_value','vehicle_value','property_value','other_assets','cash_at_hand','bank_balance_1','accounts_receivable'].reduce((a,k)=>a+n(state[k]),0);state.total_liabilities=['supplier_liabilities','bank_loans','other_borrowings'].reduce((a,k)=>a+n(state[k]),0);state.net_worth=state.total_assets-state.total_liabilities}function repayment(){const p=n(state.amount),r=n(state.annual_rate)/1200,t=n(state.tenure);return p&&t?(r?p*r*Math.pow(1+r,t)/(Math.pow(1+r,t)-1):p/t):0}function page(){calc();if(step===0)return `<div class="grid">${input('full_name','Full Name','text',true)}${input('phone','Phone Number','tel',true)}${input('nin','NIN','text',true)}${input('bvn','BVN','text',true)}${input('residential_address','Residential Address','text',true)}</div>`;if(step===1)return `<div class="grid">${input('loan_product','Loan Product','text',true)}${input('purpose','Loan Purpose')}${input('amount','Requested Amount','text',true)}${input('tenure','Tenor (months)','number',true)}${input('annual_rate','Annual Interest Rate (%)','number',true)}<div class="metric"><small>Estimated Monthly Repayment</small><b>${money(repayment())}</b></div></div>`;if(step===2)return `<div class="grid">${input('business_name','Business Name','text',true)}${input('business_type','Business Type')}${input('years_in_business','Years in Business','number')}${input('average_daily_sales','Average Daily Sales','text',true)}${input('business_days','Days of Operation per Month','number',true)}<div class="metric"><small>Monthly Sales</small><b>${money(state.monthly_sales)}</b></div></div>`;if(step===3)return `<div class="grid">${input('monthly_purchases','Monthly Purchases')}${input('rent','Rent')}${input('salaries','Salaries')}${input('utilities','Utilities')}${input('transport','Transport')}${input('other_business_expenses','Other Business Expenses')}<div class="metric"><small>Operating Cash Flow</small><b>${money(state.monthly_sales-n(state.monthly_purchases)-n(state.rent)-n(state.salaries)-n(state.utilities)-n(state.transport)-n(state.other_business_expenses))}</b></div></div>`;if(step===4)return `<div class="card"><h3>Products</h3><p class="muted">Product rows will calculate gross profit and margin automatically.</p><button class="secondary" id="addProduct">+ Add Product</button><div id="products"></div></div>`;if(step===5)return `<div class="grid">${input('equipment_value','Equipment')}${input('vehicle_value','Vehicle')}${input('property_value','Property')}${input('other_assets','Other Assets')}${input('cash_at_hand','Cash at Hand')}${input('bank_balance_1','Bank Balance')}${input('accounts_receivable','Receivables')}${input('supplier_liabilities','Supplier Liabilities')}${input('bank_loans','Bank Loans')}${input('other_borrowings','Other Borrowings')}<div class="metric"><small>Total Assets</small><b>${money(state.total_assets)}</b></div><div class="metric"><small>Total Liabilities</small><b>${money(state.total_liabilities)}</b></div><div class="metric"><small>Net Worth</small><b>${money(state.net_worth)}</b></div></div>`;if(step===6)return `<div class="grid">${input('collateral_type','Collateral Type')}${input('owner_name','Owner Name')}${input('description','Description')}${input('location','Location')}${input('market_value','Market Value')}${input('forced_sale_value','Forced Sale Value')}</div>`;return `<div class="card"><h3>${esc(state.full_name||'Customer')}</h3><p>Requested: <b>${money(state.amount)}</b></p><p>Monthly Sales: <b>${money(state.monthly_sales)}</b></p><p>Net Worth: <b>${money(state.net_worth)}</b></p><p class="muted">Ready for Supervisor review.</p></div>`}function saveLocal(){localStorage.setItem('loanappraise_v2_draft',JSON.stringify(state))}function loadLocal(){try{Object.assign(state,JSON.parse(localStorage.getItem('loanappraise_v2_draft')||'{}'))}catch{}}function validate(){let ok=true;document.querySelectorAll('[required]').forEach(x=>{x.classList.toggle('invalid',!x.value.trim());if(!x.value.trim())ok=false});for(const k of ['nin','bvn']){const x=document.querySelector(`[data-key="${k}"]`);if(x&&(x.value.replace(/\D/g,'').length!==11)){x.classList.add('invalid');ok=false}}if(step===1&&n(state.amount)<=0){document.querySelector('[data-key="amount"]')?.classList.add('invalid');ok=false}return ok}function formPayload(){calc();return {customer:{full_name:state.full_name,phone:state.phone,nin:state.nin,bvn:state.bvn,residential_address:state.residential_address},loan:{loan_product:state.loan_product,purpose:state.purpose,amount:n(state.amount),tenure:n(state.tenure),annual_rate:n(state.annual_rate)},business:{business_name:state.business_name,business_type:state.business_type,years_in_business:n(state.years_in_business),average_daily_sales:n(state.average_daily_sales),business_days:n(state.business_days),monthly_sales:state.monthly_sales},financials:{monthly_purchases:n(state.monthly_purchases),rent:n(state.rent),salaries:n(state.salaries),utilities:n(state.utilities),transport:n(state.transport),other_business_expenses:n(state.other_business_expenses)},products:state.products||[],balance:{total_assets:state.total_assets,total_liabilities:state.total_liabilities,net_worth:state.net_worth},collateral:{collateral_type:state.collateral_type,owner_name:state.owner_name,description:state.description,location:state.location,market_value:n(state.market_value),forced_sale_value:n(state.forced_sale_value)}}}async function persist(status){if(!client||!session)return null;const p=formPayload();const payload={officer_id:session.user.id,status,...p,updated_at:new Date().toISOString()};let q=client.from('loan_applications');if(state.application_id)q=q.update(payload).eq('id',state.application_id).select().single();else q=q.insert(payload).select().single();const {data,error}=await q;if(error)throw error;if(data?.id)state.application_id=data.id;return data}async function saveDraft(){saveLocal();try{await persist('draft');toast('Draft saved');}catch(e){console.error(e);toast('Draft saved locally. Database save could not be completed.')} }async function submitApplication(){if(!validate())return;saveLocal();try{await persist('pending_supervisor');toast('Application submitted to Supervisor');setTimeout(dashboard,500)}catch(e){console.error(e);toast('Submission failed: '+e.message)}}function toast(msg){let t=document.getElementById('toast');if(!t){t=document.createElement('div');t.id='toast';document.body.appendChild(t)}t.textContent=msg;t.className='toast show';setTimeout(()=>t.className='toast',2500)}function renderWizard(){calc();app.innerHTML=`<div class="shell"><aside class="side"><div class="brand">Loan<span>Appraise</span></div><div class="nav"><button class="active">New Application</button><button id="logout">Sign Out</button></div></aside><main class="main"><div class="top"><div><h1>New Loan Application</h1><div class="muted">Step ${step+1} of ${steps.length}</div></div><button class="secondary" id="saveDraft">Save Draft</button></div><div class="wizard"><div class="steps">${steps.map((s,i)=>`<button class="step ${i===step?'active':''}" data-step="${i}">${i+1}. ${s}</button>`).join('')}</div><div class="form">${page()}<div class="actions"><button class="secondary" id="back" ${step===0?'disabled':''}>Back</button>${step===7?'<button class="primary" id="submit">Submit to Supervisor</button>':'<button class="primary" id="next">Save & Continue</button>'}</div></div></div></main></div>`;document.querySelectorAll('[data-step]').forEach(b=>b.onclick=async()=>{await saveDraft();step=Number(b.dataset.step);renderWizard()});document.getElementById('saveDraft').onclick=saveDraft;document.getElementById('back').onclick=async()=>{await saveDraft();step=Math.max(0,step-1);renderWizard()};document.getElementById('next')?.addEventListener('click',async()=>{if(validate()){await saveDraft();step=Math.min(7,step+1);renderWizard()}});document.getElementById('submit')?.addEventListener('click',submitApplication);document.getElementById('logout').onclick=async()=>{await client?.auth.signOut();location.reload()}}
async function dashboard(){let apps=[];try{if(client&&session)apps=await LoanAppraiseDB.mine(client,session.user)}catch(e){console.error(e)}app.innerHTML=`<div class="shell"><aside class="side"><div class="brand">Loan<span>Appraise</span></div><div class="nav"><button class="active">Dashboard</button><button id="new">+ New Application</button><button id="logout">Sign Out</button></div></aside><main class="main"><div class="top"><div><h1>Loan Officer Dashboard</h1><div class="muted">${esc(profile?.email||'Loan Officer')}</div></div><button class="primary" id="new2">+ New Application</button></div><div class="cards"><div class="card"><div class="muted">Drafts</div><div class="stat">${apps.filter(x=>x.status==='draft').length}</div></div><div class="card"><div class="muted">Pending Supervisor</div><div class="stat">${apps.filter(x=>x.status==='pending_supervisor').length}</div></div><div class="card"><div class="muted">Returned</div><div class="stat">${apps.filter(x=>x.status==='returned').length}</div></div><div class="card"><div class="muted">Approved/Disbursed</div><div class="stat">${apps.filter(x=>['approved','disbursed','active'].includes(x.status)).length}</div></div></div><div class="card"><h3>My Applications</h3><div class="table-wrap">${apps.length?apps.map(x=>`<div class="row"><b>${esc(x.customer_data?.full_name||'Customer')}</b><span>${esc(x.status||'')}</span><strong>${money(x.loan_request?.amount)}</strong></div>`).join(''):'<p class="muted">No applications yet.</p>'}</div></div></main></div>`;document.getElementById('new').onclick=start;document.getElementById('new2').onclick=start;document.getElementById('logout').onclick=async()=>{await client?.auth.signOut();location.reload()}}
function start(){loadLocal();step=0;renderWizard()}async function init(){try{if(window.supabase?.createClient&&window.SUPABASE_URL&&window.SUPABASE_ANON_KEY)client=window.supabase.createClient(window.SUPABASE_URL,window.SUPABASE_ANON_KEY);if(client){const r=await client.auth.getSession();session=r.data.session}if(!session){login();return}profile=session.user;await dashboard()}catch(e){console.error(e);login()}}function login(){app.innerHTML=`<div class="login"><div class="login-card"><h1>Loan<span>Appraise</span></h1><p class="muted">Secure loan origination platform</p><form id="login"><div class="field"><label>Email</label><input id="email" type="email" required></div><div class="field"><label>Password</label><input id="password" type="password" required></div><div id="err" class="error"></div><button class="primary">Sign In</button></form></div></div>`;document.getElementById('login').onsubmit=async e=>{e.preventDefault();const em=document.getElementById('email').value.trim(),pw=document.getElementById('password').value;if(!client){document.getElementById('err').textContent='Authentication service unavailable.';return}const {data,error}=await client.auth.signInWithPassword({email:em,password:pw});if(error){document.getElementById('err').textContent=error.message;return}session=data.session;profile=data.user;await dashboard()}}init();